from lib.grid.libGrid import libCoord

class libTerra:
    def __init__(self, strName = "-", lstIcon = [0], intVolume = 1):
        self.strName   = str(strName)
        self.lstIcon   = lstIcon
        self.intVolume = intVolume
