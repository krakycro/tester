from lib.grid.libGrid import libCoord

class libTerra:
    def __init__(self, lstIcon = [0], intVolume = 1, libCoord = libCoord(0, 0) ):
        self.lstIcon  = lstIcon
        self.intVolume = intVolume
        self.libCoord = libCoord